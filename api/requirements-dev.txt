# Development / CI-only dependencies (not needed in production)
-r requirements.txt
pytest>=8.0,<9.1
pytest-asyncio>=0.23,<2.0
pytest-html>=4.0,<5.0
# pydantic-ai and pydantic-evals conflict with fastapi==0.115.5 via starlette.
# Install them locally with: pip install pydantic-ai pydantic-evals
# then: pip install "starlette>=0.40.0,<0.42.0" "httpx>=0.27,<0.28" --force-reinstall
